[build]
builder = "DOCKERFILE"
dockerfilePath = "WatchTogether.Backend/Dockerfile"

[deploy]
startCommand = ""
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
